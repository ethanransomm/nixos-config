# ── YOUR EXISTING config.fish GOES HERE ──
# Replace this whole file with your backed-up ~/.config/fish/config.fish
# (from arch-backup-*/home/config.fish or config/fish/config.fish).
#
# Anything Home Manager already sets (aliases, greeting, fastfetch-on-launch,
# starship) is in home/programs/shell.nix — so you can either:
#   • paste ONLY your extra functions/abbreviations/env here, or
#   • paste the whole thing and remove the duplicate bits from shell.nix.
#
# This file is sourced after HM's generated config, so your settings win.

# example of the kind of thing that lives here:
# abbr -a gc 'git commit'
# abbr -a gs 'git status'
# set -gx EDITOR nvim
